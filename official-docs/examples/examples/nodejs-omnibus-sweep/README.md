# This Example Has Moved

The omnibus sweep example has been consolidated into the comprehensive server wallets demo.

**New location:** [nodejs-server-wallets](../nodejs-server-wallets/)

The omnibus sweep workflow is now at:

- `nodejs-server-wallets/src/examples/omnibus-sweep.ts`

Run it with:

```bash
cd ../nodejs-server-wallets
pnpm install
pnpm example:omnibus
```
